package fathertoast.crust.common.util.annotations;

/**
 * Decorative annotation used to
 * mark classes/fields/methods as
 * only being referenced on client.
 *
 * (This does not strip away
 *  code from ANY logical sides)
 */
public @interface OnClient {
}
